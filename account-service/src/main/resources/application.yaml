server:
  port: 8081

spring:
  application:
    name: account-service
  profiles:
    active: dev
  datasource:
    url: 'jdbc:mysql://mysql-container:3306/amall'
    username: root
    password: '0424'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
          format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761
  instance:
    ip-address: localhost
    prefer-ip-address: true

amall:
  jwt:
    location: classpath:amall.jks
    alias: amall
    password: amall123
    tokenTTL: 30m
